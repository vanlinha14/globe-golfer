import React from 'react'
import { View } from 'react-native'

export default Separator = <View key="separator" style={{
    marginTop: 12,
    width: '100%',
    height: 1,    
    backgroundColor: Theme.separator
}}/>